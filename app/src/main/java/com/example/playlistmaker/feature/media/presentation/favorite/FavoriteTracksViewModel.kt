package com.example.playlistmaker.feature.media.presentation.favorite

import androidx.lifecycle.ViewModel

class FavoriteTracksViewModel : ViewModel() {
    // Пока экран не содержит бизнес-логики.
    // ViewModel оставлен для будущего расширения (список избранных треков)
}